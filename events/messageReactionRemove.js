module.exports = (messageReaction, user) => {
    /*
    const message = messageReaction.message;
    const member = message.guild.members.get(user.id)
    const channel = message.guild.channels.find(c => c.name === "test-bot")
    const roletTruand = message.guild.roles.fetch("443785307503067136");
    const roleMystique = message.guild.roles.fetch("671419196986621983");
    const roleChercheur = message.guild.roles.fetch("443451274730405889");
    const roleSurvivant = message.guild.roles.fetch("513969962809819146");
    const roleGardien = message.guild.roles.fetch("693864816909090838");
    if (member.user.bot) return;

    if (["ClassRogue", "ClassMystic", "ClassSeeker", "ClassSurvivor", "ClassGuardian"].includes(messageReaction.emoji.name) && message.channel.id === channel.id) {
        switch (messageReaction.emoji.name) {
            case "ClassRogue":
                member.removeRole(roletTruand);
                message.channel.send(`Le rôle ${roletTruand.name} a été retiré avec succès`).then(msg => msg.delete({ timeout: 2500 }));
                break;
            case "ClassMystic":
                member.removeRole(roleMystique);
                message.channel.send(`Le rôle ${roleMystique.name} a été retiré avec succès`).then(msg => msg.delete({ timeout: 2500 }));
                break;
            case "ClassSeeker":
                member.removeRole(roleChercheur);
                message.channel.send(`Le rôle ${roleChercheur.name} a été retiré avec succès`).then(msg => msg.delete({ timeout: 2500 }));
                break;
            case "ClassSurvivor":
                member.removeRole(roleSurvivant);
                message.channel.send(`Le rôle ${roleSurvivant.name} a été retiré avec succès`).then(msg => msg.delete({ timeout: 2500 }));
                break;
            case "ClassGuardian":
                member.removeRole(roleGardien);
                message.channel.send(`Le rôle ${roleGardien.name} a été retiré avec succès`).then(msg => msg.delete({ timeout: 2500 }));
                break;
        }

    }
    */
}